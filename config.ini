# config.ini - HOL-2734
# Version 1.2 2026-06-22
# Changes:
# - Converted to VVF: renamed [VCF]->[VVF], [VCFFINAL]->[VVFFINAL]
# - Renamed vcf* keys to vvf* in VVF sections
# - Added all missing hosts to vvfmgmtcluster: esx-08a to esx-11a (Site A),
#   esx-01b to esx-10b (Site B) — all 21 ESXi hosts now included
# - vvfpostedgevms: removed auto-platform-a (VCF Automation), added license-b
# - vvfvCenter: replaced vc-wld01-a with vc-mgmt-b; added vvfmgmtdatastore for both sites
# - vvfvspvms: added Site B entry (vsp-01b-.*:vc-mgmt-b)
# - [VCFFINAL] replaced with [VVFFINAL]: VVF-specific vcfcomponents list, active
#   vspcontrolplaneips for both sites, removed Tanzu/VCF Automation/SDDC/Fleet keys
# - [SHUTDOWN]: removed fleet_lcm_fqdn, fleet_products, vm_patterns, workload_vms,
#   and VCF Operations VM override blocks (VCF-only, not used by VVFshutdown.py)
# Author - Burke Azbill and HOL Core Team
#
# This file is copied to /tmp/config.ini by labstartup.sh
# Section and option names are case-sensitive
#
# Cross-reference key:
#   Startup modules:  Startup/prelim.py, ESXi.py, vSphere.py, VVF.py,
#                     services.py, Kubernetes.py, VVFfinal.py, final.py,
#                     pings.py, urls.py, odyssey.py
#   Shutdown modules: Shutdown/Shutdown.py, VVFshutdown.py
#   Tools:            Tools/vpodchecker.py, confighol-9.1.py,
#                     generate_labdetails.py, offline-ready.py, tdns_import.py,
#                     labtypes.py

#==============================================================================
# [VPOD] - Lab identity and global settings
# Read by: Startup/prelim.py, final.py, lsfunctions.py (init)
#==============================================================================

[VPOD]
# Lab SKU - must match the repository name
# Read by: lsfunctions.py init(), prelim.py, Tools/generate_labdetails.py
vPod_SKU = HOL-2734

# Lab type determines the startup sequence and features
# Valid values: HOL, DISCOVERY, VXP, ATE, EDU
# Read by: lsfunctions.py init(), Shutdown/Shutdown.py (selects VCF shutdown path),
#          Tools/labtypes.py, Tools/generate_labdetails.py
labtype = HOL

# Whether to lock holuser account in production (security)
# Read by: Tools/offline-ready.py (sets to false for offline prep)
# NOTE: Startup/final.py uses holuser_lock (below), not lockholuser
lockholuser = true

# Maximum minutes before lab startup fails
# Read by: lsfunctions.py init() (startup timeout watchdog)
maxminutes = 60

# Enable Odyssey client installation (VLP feature)
# Read by: Startup/odyssey.py
odyssey = false

# Interval for labcheck runs (minutes, 0 = disabled)
# UNUSED - not currently consumed by any script
# See labcheck_enabled below for the boolean toggle
# labcheckinterval = 15

# Custom conky title (if not specified, uses vPod_SKU)
# Read by: lsfunctions.py (desktop widget display)
# conky_title = NOT APPLICABLE TO HOL LABS

# Oh-My-Posh Theme Toggle -- Learn more about oh-my-posh at https://ohmyposh.dev
# The lab console and manager have oh-my-posh installed, but disabled by default.
# It uses the holoconsole-notime.omp.json (saved locally as holoconsole.omp.json) from https://github.com/burkeazbill/DimensionQuestDotFiles/blob/main/OhMyPosh/README.md
# If true, the oh-my-posh theme will be enabled and the holoconsole.omp.json file will be used.
# If false, the oh-my-posh theme will be disabled and the default theme will be used.
# Read by: .bashrc on manager and console VMs
#ohmyposh_enabled = true

# Install Playwright on the manager VM (global, all users) during startup.
# Installs the playwright Python package and chromium browser if not already present.
# Set to true if your lab requires browser automation on the manager (e.g. Authentik
# workflows, E2E testing). Also auto-enabled when authentik_vcf_integration = true.
# Read by: Startup/prelim.py Task 10
# install_playwright = false

# Enable VS Code proxy configuration on the console VM (default: true)
# Requires allowlist entries:
# VS Code Marketplace (required for extension browsing/install)
#  marketplace.visualstudio.com
#  visualstudio.com
#  vsassets.io
#  vscode-unpkg.net
#  vscode-cdn.net
#  vscode.dev
# When true, configures http.proxy and http.noProxy in VS Code settings
# for the holuser account on the console, enabling Marketplace access
# through the holorouter Squid proxy while bypassing internal subnets
# Read by: Startup/prelim.py
enablevscodeproxy = false

# Enable/disable labcheck periodic runs (default: true)
# Read by: Startup/final.py
#labcheck_enabled = true

# Enable/disable holuser account lock in production (default: follows lockholuser)
# Read by: Startup/final.py
#holuser_lock = true

# DNS record import format: zone,name,type,value
# THIS ONLY APPLIES ONCE a Technitium DNS Holorouter is incorporated into pod
# Multiple records separated by newlines (with indentation) or semicolons
# Read by: Tools/tdns_import.py
# Uncomment and add records as needed:
# new-dns-records = site-a.vcf.lab,gitlab,A,10.1.10.211
#     site-a.vcf.lab,harbor,A,10.1.10.212

#==============================================================================
# [RESOURCES] - Components verified/started during Startup
# Read by: Startup/ESXi.py, vSphere.py, services.py, pings.py, urls.py,
#          Kubernetes.py, final.py, VVFfinal.py
# Shutdown: Shutdown/VVFshutdown.py reads vCenters in Phase 2 (main connect)
#==============================================================================

[RESOURCES]
# ESXi hosts to verify SSH (port 22) and maintenance mode at startup
# Format: hostname:maintenance_mode_on_off (yes=enter MM, no=exit MM)
# Startup: ESXi.py checks SSH, vSphere.py manages maintenance mode
# Shutdown: NOT used directly; ESXi hosts come from [VVF] vvfmgmtcluster
# Tools: vpodchecker.py, confighol-9.1.py
ESXiHosts = esx-01a.site-a.vcf.lab:no
  esx-02a.site-a.vcf.lab:no
  esx-03a.site-a.vcf.lab:no
  esx-04a.site-a.vcf.lab:no
  esx-05a.site-a.vcf.lab:no
  esx-06a.site-a.vcf.lab:no
  esx-07a.site-a.vcf.lab:no
  esx-08a.site-a.vcf.lab:no
  esx-09a.site-a.vcf.lab:no
  esx-10a.site-a.vcf.lab:no
  esx-11a.site-a.vcf.lab:no
  esx-01b.site-b.vcf.lab:no
  esx-02b.site-b.vcf.lab:no
  esx-03b.site-b.vcf.lab:no
  esx-04b.site-b.vcf.lab:no
  esx-05b.site-b.vcf.lab:no
  esx-06b.site-b.vcf.lab:no
  esx-07b.site-b.vcf.lab:no
  esx-08b.site-b.vcf.lab:no
  esx-09b.site-b.vcf.lab:no
  esx-10b.site-b.vcf.lab:no
  
# vCenters to connect to during startup
# Format: hostname:type:sso_user (type: linux, windows, esx)
# Startup: vSphere.py connects, checks datastores/clusters/VMs
# Shutdown: VVFshutdown.py Phase 2 (main connect), Phase 5 (ESXi fallback)
# Tools: vpodchecker.py, confighol-9.1.py, VVFfinal.py
vCenters = vc-mgmt-a.site-a.vcf.lab:linux:administrator@vsphere.local
  #vc-wld01-a.site-a.vcf.lab:linux:administrator@wld.sso
  vc-mgmt-b.site-b.vcf.lab:linux:administrator@vsphere.local
  #vc-wld01-b.site-b.vcf.lab:linux:administrator@wld2.sso

# Datastores to verify (hosts rescanned if missing)
# Format: VSAN:datastore_name or storage_host:datastore_name
# Startup: vSphere.py
Datastores = VSAN:vsan-mgmt-01a
  VSAN:vsan-mgmt-01b
  #stg-01a.site-a.vcf.lab:ISCSI01-COMP01B
  #VSAN:RegionA01-VSAN-COMP01A
  #VSAN:RegionB01-VSAN-COMP01B

# DRS configuration at startup
# Format: clustername:on|off
# Startup: vSphere.py
Clusters = cluster-mgmt-01a:off
  #cluster-wld01-01a:off
  cluster-mgmt-01b:off
  #RegionB01-COMP01B:off

# Nested VMs to power on during startup
# Format: vmname:vcenter_or_esxhost (supports regex patterns)
# Startup: vSphere.py powers on in listed order
# Shutdown: NOT used; VMs shut down by VVFshutdown.py phases
# Tools: confighol-9.1.py
VMs = vsp-01a-.*:vc-mgmt-a.site-a.vcf.lab
  ops.*:vc-mgmt-a.site-a.vcf.lab
  vsp-01b-.*:vc-mgmt-b.site-b.vcf.lab
  # sddcmanager-a:vc-mgmt-a.site-a.vcf.lab
  dpca-mgmt-a:vc-mgmt-a.site-a.vcf.lab
  dpca-mgmt-b:vc-mgmt-b.site-b.vcf.lab
  salt-a:vc-mgmt-a.site-a.vcf.lab
  acct*:vc-mgmt-a.site-a.vcf.lab
  sales*:vc-mgmt-a.site-a.vcf.lab
  # dev*:vc-mgmt-a.site-a.vcf.lab


# vApps to power on during startup
# Format: vappname:vcenter
# Startup: vSphere.py
#vApps = YourvApp:vc-mgmt-01a.site-a.vcf.lab
 # YourOthervApp:vc-mgmt-01a.site-a.vcf.lab

# IP addresses to ping-check during startup and final verification
# Startup: pings.py, final.py
Pings = 10.1.10.129
  10.1.10.131

# Windows services to verify/start
# Format: server:service:passwd:waitsec
# Startup: services.py (LEGACY - no current Python consumer)
#WindowsServices = server:service:passwd:waitsec
 # Site A SRM embedded database
 #srm-01a.site-a.vcf.lab:vmware-dr-vpostgres::10
 # Site A SRM server
 #srm-01a.site-a.vcf.lab:vmware-dr::10
 # Site B SRM embedded database::10
 #srm-01b.site-b.vcf.lab:vmware-dr-vpostgres::10
 # Site B SRM server
 #srm-01b.site-b.vcf.lab:vmware-dr::10

# Linux services to verify/start
# Format: server:service:passwd:waitsec
# Startup: services.py
#LinuxServices = server:service:passwd:waitsec
 # For vSphere 6.7 and 7.x, uncomment the vcsa-01x lines as needed
 # For vSphere 8.x, leave the vcsa-01x lines commented out
 # example to check vSphere ui service (site A)
 #vc-mgmt-01a.site-a.vcf.lab:vsphere-ui::5
 # example to check vSphere ui service (site B)
 #vc-mgmt-01b.site-b.vcf.lab:vsphere-ui::5

# TCP ports to verify during startup
# Format: hostname:port
# Startup: services.py
TCPServices = vc-mgmt-a.site-a.vcf.lab:443
  #vc-wld01-a.site-a.vcf.lab:443
  vc-mgmt-b.site-b.vcf.lab:443

# Kubernetes certificate renewal
# Format: host:account:password:renewal_command
# Startup: Kubernetes.py
#Kubernetes = k8s-master.site-a.vcf.lab:root:******:kubeadm certs renew all
  #k8s-master.site-a.vcf.lab:root:******:kubeadm alpha certs renew all

# URLs to check for expected response text
# Format: url,expected_text (200 status checked if no text specified)
# Startup: urls.py, final.py
# Tools: generate_labdetails.py, offline-ready.py
URLS = https://www.vmware.com/,VMware
  # VCF 9 base templates
  https://vc-mgmt-a.site-a.vcf.lab/ui/,loading-container
  https://vc-mgmt-a.site-a.vcf.lab:5480/#/login,VMware vCenter Management
  #https://vc-wld01-a.site-a.vcf.lab/ui/,loading-container
  #https://vc-wld01-a.site-a.vcf.lab:5480/#/login,VMware vCenter Management
  https://ops-a.site-a.vcf.lab/ui/,Operations
  #https://sddcmanager-a.site-a.vcf.lab/ui/,SDDC Manager
  https://vc-mgmt-b.site-b.vcf.lab/ui/,loading-container
  #http://stg-01a.site-a.vcf.lab/account/login,TrueNAS
  https://dpca-mgmt-a.site-a.vcf.lab/,LAUNCH PROTECTION
  https://dpca-mgmt-b.site-b.vcf.lab/,LAUNCH PROTECTION
  https://salt-a.site-a.vcf.lab/login/,Salt

#==============================================================================
# [VVF] - VVF infrastructure components (ESXi, vCenter, license servers, VSP)
# Startup: Startup/VVF.py (Phases 1-5: connect hosts, check datastores,
#          post-edge VMs (license servers), vCenter)
# Shutdown: Shutdown/VVFshutdown.py reads this section for:
#   - vvfmgmtcluster -> Phases 4, 7, 8 (ESXi direct connect, vSAN, host shutdown)
#   - vvfvCenter     -> Phase 5 (vCenter VM shutdown via ESXi direct)
#   - vvfpostedgevms -> Phase 6 (license VM shutdown via ESXi direct, AFTER vCenter)
#   - vvfvspvms      -> Phase 3 (VSP VM shutdown via vCenter, BEFORE ESXi direct)
# Tools: confighol-9.1.py configure ESXi, vCenter, ops VMs
#==============================================================================

[VVF]

# ESXi hosts that host VVF management cluster (both sites)
# Format: hostname:type (type is always 'esx')
# Startup: VVF.py Phase 1 - connect, exit maintenance mode
# Shutdown: VVFshutdown.py Phase 4 (ESXi direct connect),
#           Phase 7 (vSAN elevator), Phase 8 (host shutdown)
vvfmgmtcluster = esx-01a.site-a.vcf.lab:esx
  esx-02a.site-a.vcf.lab:esx
  esx-03a.site-a.vcf.lab:esx
  esx-04a.site-a.vcf.lab:esx
  esx-05a.site-a.vcf.lab:esx
  esx-06a.site-a.vcf.lab:esx
  esx-07a.site-a.vcf.lab:esx
  esx-08a.site-a.vcf.lab:esx
  esx-09a.site-a.vcf.lab:esx
  esx-10a.site-a.vcf.lab:esx
  esx-11a.site-a.vcf.lab:esx
  esx-01b.site-b.vcf.lab:esx
  esx-02b.site-b.vcf.lab:esx
  esx-03b.site-b.vcf.lab:esx
  esx-04b.site-b.vcf.lab:esx
  esx-05b.site-b.vcf.lab:esx
  esx-06b.site-b.vcf.lab:esx
  esx-07b.site-b.vcf.lab:esx
  esx-08b.site-b.vcf.lab:esx
  esx-09b.site-b.vcf.lab:esx
  esx-10b.site-b.vcf.lab:esx

# VVF management datastore names (verified during startup)
# Startup: VVF.py Phase 2 - verify datastore accessible
vvfmgmtdatastore = vsan-mgmt-01a
  vsan-mgmt-01b

# Post-Edge VMs - license servers must be powered on BEFORE vCenter
# and powered off ONLY AFTER vCenter has been fully shut down.
# Power operations always use direct ESXi connections (never vCenter API).
# Format: vmname
# Startup: VVF.py Phase 4b - power on via ESXi direct before vCenter
# Shutdown: VVFshutdown.py Phase 6 - shutdown via ESXi direct, AFTER vCenter
vvfpostedgevms = license-a
  license-b

# VVF vCenter VMs (VM display names as seen in vSphere)
# Format: vcenter-vm-name
# Startup: VVF.py Phase 5 - power on vCenter (host-agnostic search via ESXi direct)
# Shutdown: VVFshutdown.py Phase 5 - shutdown via ESXi direct (NOT vCenter API)
# Tools: confighol-9.1.py
vvfvCenter = vc-mgmt-a
  vc-mgmt-b

# VSP Platform VMs (regex pattern) - host the VCF Fleet management cluster
# Format: vm_regex:vcenter
# Startup: VVFfinal.py Task 1 - power on (via vCenter, already running at that point)
# Shutdown: VVFshutdown.py Phase 3 - graceful shutdown via vCenter (vCenter still up),
#           vcf_services_runtime_shutdown.sh in Phase 1 also powers these off via govc
vvfvspvms = vsp-01a-.*:vc-mgmt-a.site-a.vcf.lab
  vsp-01b-.*:vc-mgmt-b.site-b.vcf.lab

# VVF-specific URLs to check (separate from RESOURCES URLs)
# Format: url,expected_text
# Tools: vpodchecker.py, confighol-9.1.py
#urls = https://fleet-01a.site-a.vcf.lab/fleet-lcm/v1/components

#==============================================================================
# [VVFFINAL] - Final VVF startup tasks (VSP cluster, VCF Fleet components)
# Startup: Startup/VVFfinal.py (runs after VVF.py and vSphere.py complete)
# Shutdown: Shutdown/VVFshutdown.py reads this section for:
#   - vcfcomponents      -> Phase 1 (vcf_services_runtime_shutdown.sh graceful drain)
#   - vspcontrolplaneips -> Phase 1 (one call per site for the shutdown script)
#==============================================================================

[VVFFINAL]

# VCF Component Services on VSP management cluster (K8s workloads)
# These are the Fleet/LCM components that run on the VSP cluster in VVF.
# Format: namespace:resource_type/resource_name
# Startup:  VVFfinal.py Task 2 - waits for auto-recovery via power-off-marker
#           (vcf_services_runtime_shutdown.sh sets marker on shutdown)
# Shutdown: VVFshutdown.py Phase 1 - calls vcf_services_runtime_shutdown.sh
#           which gracefully drains all workloads via port-5480 API,
#           then powers off VSP VMs via govc
vcfcomponents = telemetry:deployment/telemetry-acceptor
  vcf-fleet-depot:deployment/depot-service
  vcf-fleet-depot:deployment/distribution-service
  vcf-fleet-lcm:deployment/vcf-fleet-build-service-fleetbuild
  vcf-fleet-lcm:deployment/vcf-fleet-upgrade-service-fleetupgrade
  vcf-fleet-lcm:statefulset/vcf-fleet-lcm-db
  vcf-sddc-lcm:deployment/vcf-sddc-build-service-sddcbuild
  vcf-sddc-lcm:deployment/vcf-sddc-upgrade-service-sddcupgrade
  vcf-sddc-lcm:statefulset/vcf-sddc-lcm-db

# VSP cluster management API VIPs (port 5480) — one per site
# Used by: VVFfinal.py Task 2 (poll API health after VSP VM power-on)
#          VVFshutdown.py Phase 1 (call vcf_services_runtime_shutdown.sh per site)
# The script is called once per VIP: --node-ip <vip> authenticates to the
# cluster management API and orchestrates graceful shutdown + VM power-off.
vspcontrolplaneips = 10.1.1.142
  10.2.1.142

# VCF Component URLs to check after startup (Fleet endpoints)
# Format: url,expected_text  (401 also accepted = service up, auth required)
# Startup: VVFfinal.py Task 4
vcfcomponenturls = https://fleet-01a.site-a.vcf.lab/fleet-lcm/v1/components
  https://fleet-01b.site-b.vcf.lab/fleet-lcm/v1/components

#==============================================================================
# [AUTHENTIK] - Authentik identity provider user and group provisioning
# Runs during Startup/prelim.py Task 12 (early startup, pre-VVF).
# All operations are idempotent — safe to re-run on every boot.
# Read by: Startup/prelim.py Task 12
#==============================================================================

[AUTHENTIK]

# Authentik base URL
# Override only if your deployment uses a non-standard hostname.
# Default: https://auth.vcf.lab
# authentik_base_url = https://auth.vcf.lab

# Groups to create in Authentik (one per line).
# Format: groupname
# authentik_groups = dev-team
#   ops-team
#   lab-viewers
#   approvers

# Users to create in Authentik (one per line).
# Format: username:Display Name:email:group1,group2,...
# Password: all users are assigned the lab password from /home/holuser/creds.txt.
# Idempotent: if a user with the same email already exists, creation is skipped.
# Read by: Startup/prelim.py Task 12
# authentik_users = alice:Alice Smith:alice@vcf.lab:dev-team,lab-viewers
#   bob:Bob Jones:bob@vcf.lab:ops-team
#   charlie:Charlie Brown:charlie@vcf.lab:dev-team,approvers
#   dana:Dana White:dana@vcf.lab:lab-viewers

#==============================================================================
# [SHUTDOWN] - Shutdown-specific configuration
# Read by: Shutdown/VVFshutdown.py, Shutdown/Shutdown.py
# NOTE: ESXi hosts and vCenters are read from [VVF] and [RESOURCES] sections.
#       This section contains vSAN, host shutdown settings and optional Docker.
#==============================================================================

[SHUTDOWN]

# Docker containers to stop before VVF shutdown
# Shutdown: Shutdown.py Phase 0b
#shutdown_docker = true
#docker_host = docker.site-a.vcf.lab
#docker_user = holuser
#docker_containers = gitlab,ldap,poste.io,flask

# ESXi username for SSH operations (vSAN elevator)
# Shutdown: VVFshutdown.py Phase 7
esx_username = root

# vSAN settings
# Shutdown: VVFshutdown.py Phase 7 (elevator operations)
# NOTE: ESA environments are auto-detected and skip the 45-minute wait
vsan_enabled = true
# vSAN timeout in seconds (45 minutes = 2700 seconds)
vsan_timeout = 2700

# Host shutdown toggle
# Shutdown: VVFshutdown.py Phase 8
shutdown_hosts = true

# NOTE: The following are read automatically from [VVF] section:
# - ESXi Hosts:   [VVF] vvfmgmtcluster  -> Shutdown Phases 4/7/8
# - vCenter VMs:  [VVF] vvfvCenter      -> Shutdown Phase 5 (via ESXi direct)
# - License VMs:  [VVF] vvfpostedgevms  -> Shutdown Phase 6 (via ESXi direct, after vCenter)
# - VSP VMs:      [VVF] vvfvspvms       -> Shutdown Phase 3 (via vCenter before ESXi direct)
# - VSP VIPs:     [VVFFINAL] vspcontrolplaneips -> Phase 1 (shutdown script per site)
